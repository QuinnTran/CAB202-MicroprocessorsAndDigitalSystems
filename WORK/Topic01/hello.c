#include <stdio.h>
#include <stdio.h>

void print_hello( void ) {
	// (a) Use printf() to say hello to your student number, on a line by itself.
	printf( "Hello n9808281." );  
	return 0;
}

int main( void ) {
	print_hello();
	return 0;
}